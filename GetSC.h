#pragma once
#include <string>
#include <iostream>

class GetSC {
public:
    static void readT(std::string& input);

    static bool readF(std::string &input, const std::string &fileN);
};
